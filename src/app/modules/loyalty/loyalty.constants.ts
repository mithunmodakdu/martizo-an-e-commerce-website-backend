export const LOYALTY_CONFIG = {
  // Points earned per 1 unit of currency spent (e.g. 1 point per ৳100 -> 1 / 100).
  POINTS_EARN_RATE: 1 / 100,

  // Earned points expire after 365 days being credited.
  POINTS_EXPIRY_DAYS: 365,


} as const;
